# Banc de vérification du lot 3e_C9.2 / 3e_C8.3
# Usage (Docker Desktop 4.87+, PowerShell, depuis ce dossier) :
#   docker compose build
#   docker compose run --rm compile station_alerte_cyclonique
services:
  compile:
    build: .
    volumes:
      - ..:/sketchs:ro
